MONGO_URI=
JWT_SECRET=
PORT=5000

# Firebase Admin service account JSON for direct FCM push notifications.
# Paste either the full JSON string or a base64 encoded JSON string.
FIREBASE_SERVICE_ACCOUNT_JSON=
